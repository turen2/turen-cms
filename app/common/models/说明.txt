后台开发内容的多语言可以进行切换，后台的界面多语言则统一为中文（或根据需要修改为另一种语言）。
而前台界面和内容都需要多语言，并且统一使用切换的模式，前台包括：手机端、电脑端、前后分离模式等都需要多语言切换。

于是作了如下约定：
后台的模型独立于所有前台。
即各前台共同使用common/models的模型，后台使用自己搭建的模型。

如此将能解决前后台巨大的使用场景差异带来的繁琐模型兼容问题。
唯一要做的就是修改了数据库结构，需要更新两次。
